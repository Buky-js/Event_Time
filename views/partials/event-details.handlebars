<section class="card">
  <a href="/event/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">
      <p>By: {{present_by}}</p>
    </div>
  </a>
</section>